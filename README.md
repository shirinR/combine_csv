to install dependencies, `$ python -m virtualenv venv && source venv/bin/activate && pip install -r requirements.txt`

add the csvs you want to combine to this directory.

to run, `$ python csv_combiner.py`
